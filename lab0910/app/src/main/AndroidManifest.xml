<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lab0910">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_log1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".matricula.listMatriculados"
            android:label="@string/title_activity_list_matriculados"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".estudiante.listEstudiante"
            android:label="@string/title_activity_list_estudiante"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".principalMenu"
            android:label="@string/title_activity_principal_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".curso.ListCurso"
            android:label="@string/title_activity_list_curso"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".estudiante.AddEstudiante" />
        <activity android:name=".curso.AddCurso" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>